import styled from 'styled-components';

const AdvantageStyled = styled.div`
    display: flex;
    width: 618px;
    margin: 50px;

    & > img {
        margin-right: 20px;
    }

    & > p {
        font-size: 25px;
    }
`;

export default AdvantageStyled;